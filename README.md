# Web-LLM-Transcriber

## GitHub Pages deployment note

This project is built with Vite and deployed under the repository subpath.  
`vite.config.js` sets `base: '/Web-LLM-Transcriber/'` so production asset URLs resolve correctly on GitHub Pages.
